<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="shop.user.dao.UserDao">

    <select id="getUser" parameterType="string"
            resultType="java.shop.user.domain.entity.User">
        SELECT id,name,mobile,mail,password,loginStatus,lastLoginTime,lastLoginIp,registerTime,userType,account
        FROM sys_user
        WHERE 1=1 and id=#{id}
    </select>

</mapper>